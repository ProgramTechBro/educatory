import '../../models/message_model.dart';

class ChatRemoteDataSource {
  Future<MessageModel> sendMessage(String contactId, String text) {
    throw UnimplementedError('Connect to your backend API here');
  }
}
