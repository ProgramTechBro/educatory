import 'package:flutter/material.dart';

class ChatBubbleIn extends StatelessWidget {
  const ChatBubbleIn({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
